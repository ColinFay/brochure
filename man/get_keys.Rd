% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{get_keys}
\alias{get_keys}
\title{Get the keys of a parameterised page}
\usage{
get_keys(x = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{x}{A shiny session, or the \code{request} object a page \code{ui} receives.
Defaults to the session the code is running in.}
}
\value{
A named list of the values matched in the page \code{href}, or \code{NULL}
outside of a brochure page.
}
\description{
A page declared with a parameterised \code{href} (e.g. \code{"/who/:id"}) exposes the
values matched in the URL through \code{get_keys()}. Call it with no argument
inside a page \code{server}, and pass it the \code{request} inside a page \code{ui}.
}
\examples{
library(shiny)
page(
  href = "/who/:id",
  ui = function(request) {
    h1(get_keys(request)$id)
  },
  server = function(input, output, session) {
    print(get_keys())
  }
)
}
