% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brochureAppRoutr.R
\name{brochureApp}
\alias{brochureApp}
\title{Create a brochureApp}
\usage{
brochureApp(
  ...,
  onStart = NULL,
  options = list(),
  enableBookmarking = NULL,
  content_404 = "Not found",
  basepath = "",
  req_handlers = list(),
  res_handlers = list(),
  wrapped = shiny::tagList
)
}
\arguments{
\item{...}{The \code{page()}s and \code{redirect()}s of the app, plus anything to
inject \strong{as is} into every page: tags, tag lists, html dependencies and
strings. That is how you add something to all your pages at once, a
\verb{<script>} or the resources of a \code{{golem}} app for example. Note that a
string is injected too, so a stray value ends up rendered on every page.
Anything else is an error naming the element it cannot use. A bare list is
spliced, so \code{brochureApp(list(page_1(), page_2()))} builds two pages rather
than injecting the list into each of them.}

\item{onStart}{A function that will be called before the app is actually run.
This is only needed for \code{shinyAppObj}, since in the \code{shinyAppDir}
case, a \code{global.R} file can be used for this purpose.}

\item{options}{Named options that should be passed to the \code{runApp} call
(these can be any of the following: "port", "launch.browser", "host", "quiet",
"display.mode" and "test.mode"). You can also specify \code{width} and
\code{height} parameters which provide a hint to the embedding environment
about the ideal height/width for the app.}

\item{enableBookmarking}{Can be one of \code{"url"}, \code{"server"}, or
\code{"disable"}. The default value, \code{NULL}, will respect the setting from
any previous calls to  \code{\link[shiny:enableBookmarking]{enableBookmarking()}}. See \code{\link[shiny:enableBookmarking]{enableBookmarking()}}
for more information on bookmarking your app.}

\item{content_404}{The content served when no \code{page()} matches the url.}

\item{basepath}{The path your app is served under by a reverse proxy. It is
removed from the incoming url, so that what is left matches the href of your
\code{page()}, and it is prepended to the urls the app emits. For example, if your
app is served at \verb{http://connect.thinkr.fr/brochure/} and your page is named
\code{page1}, use \code{basepath = "brochure"}.}

\item{req_handlers}{a list of functions that can manipulate the \code{req} object.
These functions should take \code{req} as a parameters, and return the \code{req} object
(potentially modified), or an object of class httpResponse. If any of the
req_handlers return an httpResponse, this response will be sent to the browser
immediately, stopping any other code.}

\item{res_handlers}{A list of functions that can manipulate the httpResponse
object before it is send to the browser. Each function must take a \code{res} and
\code{req} parameter.}

\item{wrapped}{A UI function wrapping the Brochure UI.
Default is \code{shiny::tagList}.}
}
\value{
A shiny.appobj
}
\description{
This function is to be used in place of \code{shinyApp()}. It takes a series of
\code{page()}s, each with its own url, UI and server function, and serves them
from a single Shiny application.
}
\details{
Behind a reverse proxy, \code{basepath} tells the app where it is mounted: it is
removed from the incoming path, and prepended to the urls the app emits.
On Posit Connect the mount is picked up on its own, from the
\code{RStudio-Connect-App-Base-URL} header, and \code{basepath} is not needed. That
header is not part of any published contract, so set \code{basepath} if you want
the behaviour pinned. Brochure also injects a small script registering the
handler \code{server_redirect()} talks to, which prefixes internal targets with
the mount.
}
\examples{
library(shiny)

app <- brochureApp(
  page(
    href = "/",
    ui = tagList(h1("Home")),
    server = function(input, output, session) {}
  ),
  page(
    href = "/contact",
    ui = tagList(h1("Contact"))
  ),
  redirect(from = "/index.html", to = "/")
)

# Then run it as you would any Shiny app:
if (interactive()) {
  shiny::runApp(app)
}
}
\seealso{
\code{\link[=page]{page()}} to declare a page, \code{\link[=redirect]{redirect()}} to answer an url with a
redirection, and \code{vignette("deployment")} to serve the app under a prefix.
}
